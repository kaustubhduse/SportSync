apiVersion: v1
kind: Service
metadata:
  name: mongo-live-score
spec:
  selector:
    app: mongo-live-score
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
