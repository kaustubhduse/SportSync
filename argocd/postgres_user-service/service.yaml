apiVersion: v1
kind: Service
metadata:
  name: postgres_user
spec:
  selector:
    app: postgres_user
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: LoadBalancer
