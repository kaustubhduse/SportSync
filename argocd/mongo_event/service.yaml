apiVersion: v1
kind: Service
metadata:
  name: mongo_event
spec:
  selector:
    app: mongo_event
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: LoadBalancer
